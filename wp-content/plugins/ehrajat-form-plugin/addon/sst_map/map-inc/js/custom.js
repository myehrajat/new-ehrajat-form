jQuery(document).ready(function($) {

        // Basic usage
        jQuery(".placepicker").placepicker();

					   /*$text_input .='
        // Advanced usage
        jQuery("#advanced-placepicker'.$id.'").each(function() {
          var target = this;
          var $collapse = jQuery(this).parents(".form-group'.$id.'").next(".collapse'.$id.'");
          var $map = $collapse.find(".another-map-class");

          var placepicker = jQuery(this).placepicker'.$id.'({
            map: $map.get(0),
            placeChanged: function(place) {
              console.log("place changed: ", place.formatted_address, this.getLocation());
            }
          }).data("placepicker'.$id.'");
        });';

*/
});// END document.ready